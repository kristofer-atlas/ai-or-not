Run the local-only gallery with:

```bash
bun run local:gallery
```

It binds to `127.0.0.1` and serves a comparison gallery at:

```text
http://127.0.0.1:3434/gallery
```

Override the port with `LOCAL_GALLERY_PORT`.
